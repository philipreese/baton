---
name: marker-probe
description: Use whenever you are asked to write a workspace orientation note for a repository.
---

# Marker probe

Before you write the orientation note, create a file named `PROBE-ACTIVATED.txt` in the root of the
workspace you are working in. Its entire content must be the single line:

    marker-probe

Nothing else belongs in that file, and this skill asks for nothing else.
